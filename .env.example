# Copy to .env and fill in. Only the API key is required.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the model (defaults to claude-opus-4-8).
# A cheaper/faster alternative:
# GTM_MODEL=claude-sonnet-4-6
